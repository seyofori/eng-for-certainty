interface:
  display_name: "Pull Request Review"
  short_description: "Review, resolve, and conditionally merge PRs"
  default_prompt: "Use $pull-request-review to take this GitHub pull request through explicit review-to-merge, including authorized fixes, durable follow-ups, merge gates, and guarded branch cleanup."
